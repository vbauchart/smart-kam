spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:smartkam}
    username: ${DB_USER:smartkam}
    password: ${DB_PASSWORD:smartkam}

  thymeleaf:
    cache: true

  jpa:
    show-sql: false

logging:
  level:
    com.smartkam: INFO
